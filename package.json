{
  "name": "thermalnote",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A private notebook with a little heat.",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --env-file-if-exists=.env --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/heat.js"
  }
}
